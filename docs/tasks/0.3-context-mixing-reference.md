# Task 0.3 — Context-mixing reference ceiling (optional, cheap)

- Status: OPTIONAL (nice-to-have yardstick)
- Depends on: 0.2 (prequential mode)
- Branch from: `master` after 0.2 merges

## Context
A small online **context-mixing** predictor (PAQ/cmix/nncp spirit): mixture of a few cheap
sub-predictors with online-learned weights. It is a **non-candidate reference**, not an entrant
— it tells us the bpb-per-FLOP that single-pass online learning can reach (the target to approach).

## Scope
- Implement behind the same model interface so it runs under the prequential harness.
- Keep it deliberately simple (a handful of order-k byte predictors + logistic mixing).
- Plot its bpb-vs-FLOP curve on the leaderboard, clearly labelled "reference (not a candidate)".

## Acceptance criteria
- Gates green; produces a labelled reference curve; offline smoke test.
- `docs/learning/concepts/context-mixing.md` (new) + experiment entry, cross-linked.

## Deliverable
Own branch + own PR (do not merge); cross-reviewed by a different vendor.
