---
title: B.4 — delta-rule fast weights (the first non-hollow Space-B win)
---

import Layout from "../../layouts/Layout.astro";
import Callout from "../../components/Callout.astro";
import BpbFlopChart from "../../components/BpbFlopChart.astro";
import { deltaMix } from "../../data/curves";

<Layout
  title="B.4 — online delta-rule fast weights (the first non-hollow Space-B win)"
  kicker="EXP B.4"
  blurb="One online delta-rule fast-weight stream beats the cheap count mixer at matched FLOPs — the first Space-B result that is not Pareto-hollow."
  related={[
    { href: "/concepts/fast-weight-memory", title: "Fast-weight associative memory" },
    { href: "/experiments/A.1-fast-weight-memory", title: "A.1 — fast-weight memory (the bolt-on)" },
    { href: "/experiments/B.3-hashed-mix-full-corpus", title: "B.3 — hashed tables at full corpus" },
    { href: "/experiments/B.2-warmed-mixing", title: "B.2 — warmed mixing (first real win)" },
    { href: "/concepts/context-mixing", title: "Online context mixing" },
    { href: "/concepts/online-warmup", title: "Online warm-start" },
    { href: "/concepts/source-iv-advantage", title: "Source-(iv) advantage" },
    { href: "/concepts/prequential-evaluation", title: "Prequential evaluation" },
  ]}
>

<Callout variant="insight" title="the first Space-B candidate that is not Pareto-hollow">
Every prior learning-rule candidate (A.1, B.1, B.2-gated) refined an *already-good cheap learner*, and
a free/cheap online baseline dominated it per FLOP. `delta_mix` attacks a **structural weakness of the
count mixer itself** instead — and on the matched-FLOP kill-test against the cheap count baselines it
**wins**: `delta` reaches **2.4181 bpb** vs the counts-only **2.4353** and the matched-FLOP
**2.4327** — a genuine (if modest) **−0.0146 bpb** per-FLOP edge. It is the **first** Space-B result to
clear the bar that killed all the others.
</Callout>

**Status:** mechanism sound, honestly metered, cross-vendor reviewed, and — for the first time in a
Space-B (learning-rule) candidate — **NOT Pareto-hollow** at CI scale. The full-corpus carve verdict is
**pending**. **Concepts:** [fast-weight memory](/concepts/fast-weight-memory),
[online context mixing](/concepts/context-mixing), [online warm-start](/concepts/online-warmup),
[Source-(iv)](/concepts/source-iv-advantage),
[prequential evaluation](/concepts/prequential-evaluation). **Builds on:**
[B.3 — hashed tables](/experiments/B.3-hashed-mix-full-corpus).

## The bet (Source-(iv))

Every prior fast-weight / PC attempt died the same way: it *refined an already-good cheap learner*, and
a free/cheap online baseline dominated it per FLOP. So this candidate attacks a **structural weakness of
the count mixer itself**, not its compute budget.

A count table is a *degenerate associative memory* — a **one-hot-key Hebbian** store, where each exact
context owns a cell bumped by `+1`. Two hard limits follow:

1. **Zero generalization.** A never-seen k-gram makes that order *abstain*; at scale most high-order
   contexts are novel.
2. **Only $K$ global mixer weights** combine the orders — "order-3 is worth $w_3$ *everywhere*." The
   mixer cannot learn that *some* trigrams are far more predictive than others.

`delta_mix` adds **one** online predictor whose learning rule is an **error-correcting delta (LMS)
update** on a *distributed* key, so it holds $d \times V$ **per-feature, per-byte** affinities: it learns
"`tio` → `n` is a strong rule" once and shares it across every context containing `tio`, weighted by its
*measured residual utility*. It also reaches **beyond the order-`max_order` ladder** (orders 7–8 in the
key, at ~free marginal cost).

## Mechanism — one extra stream on the warmed hashed ladder

It rides on the [B.3](/experiments/B.3-hashed-mix-full-corpus) warmed hashed count ladder and adds a
single learned stream:

- **Key** $\varphi(\text{ctx})$: for each n-gram order in `delta_orders` (default 3–8), hash the suffix
  n-gram (the same Fibonacci hash the count tables use) to a bucket in $[0, d)$ with a **±1 sign** from a
  second hash (signed feature-hashing — collisions cancel in expectation). $\varphi$ is $s$-sparse.
- **Predict** $z = W\varphi$ (touches only the $s$ active columns) — one more **raw-logit** row in the
  existing logistic mixer.
- **Learn** (deferred to the next byte, so there is no leakage):
  $\Delta W_{:,j} = -\eta\,\varphi_j\,(\mathrm{softmax}(z) - \mathrm{onehot}(b))$ over the $s$ touched
  columns. This is **error-correcting**, *not* plain Hebbian (a superposed-key Hebbian write collapses to
  the byte marginal — the A.1 death; an ablation confirms delta beats it).

<Callout variant="note" title="feasibility crux (FLOP honesty)">
Sparsity makes both the read and the rank-1 write $O(sV)$, **not** $O(dV)$ — a dense key would be
~5600× the bar and dead on arrival. The table size $d$ (default $2^{18}$, ~268 MiB) costs only RAM and
collision noise, **never FLOPs**. The add-on is ≈ **8.2k FLOPs/byte** on the bar's ~15.7k; every op is
charged via `smolml.flops` (the cross-vendor review caught two real discrepancies — a sign-hash
overcharge on the unsigned path and a colliding-column write that did not accumulate; both fixed).
</Callout>

### Why it is a real (iv) claim — honestly

It *is* online gradient descent, but the model is **linear in a fixed feature map** and the loss is
**convex**, so the exact gradient is $(p - \text{target}) \otimes \varphi$ — a single rank-1 outer
product with **zero backward pass, no chain rule, no 2× tax, no bad minima**. That is the legitimate
"more loss-reduction per FLOP than SGD-on-a-net" edge; the win over the *count mixer* is the per-feature
generalization above. Contrast [A.1](/experiments/A.1-fast-weight-memory): the same fast-weight family,
but a Hebbian write bolted onto a transformer — see
[the two write-rule lineages](/concepts/fast-weight-memory).

<Callout variant="insight" title="the recurring reflex, finally cleared">
**Beating an internal variant is not a per-FLOP win — you have to beat the cheap baseline at matched
FLOPs.** That reflex — plot the cheap baselines first — exposed every false win in this project: A.1,
B.1, and B.2-gated all looked good against an internal control and then died against the free/cheap
frontier. `delta_mix` is the **first** Space-B candidate to clear it.
</Callout>

## Result — the matched-FLOP kill-test (real enwik8, 4 MB slice)

The kill-test plots the cheap baselines first and runs them **at matched total FLOPs**
($\approx 1.07\times10^{10}$): the candidate must beat **both**, or it is hollow.

| run | bpb | total FLOPs | what it isolates |
| --- | ---: | ---: | --- |
| (a) `counts_only` (hashed order-6) | 2.4353 | $1.050\times10^{10}$ | the cheap ladder at budget |
| **(b) `delta` (counts + delta)** | **2.4181** | $1.074\times10^{10}$ | the candidate |
| (c) `counts_more_warm` (hashed order-6) | 2.4327 | $1.072\times10^{10}$ | the same FLOPs, all on more warm counts |

<figure class="figure">
  <BpbFlopChart
    series={deltaMix}
    yMin={2.40}
    yMax={2.45}
    annotations={[
      { flops: 1.074e10, bpb: 2.4181, text: "delta sits below the counts-only frontier", dx: 64, dy: 18 },
      { flops: 1.072e10, bpb: 2.4327, text: "matched total FLOPs — counts lose by 0.0146 bpb", dx: 56, dy: -18 },
    ]}
  />
  <figcaption class="figcaption">
    bpb vs total FLOPs on the real `enwik8` 4 MB slice, at matched total FLOPs
    ($\approx 1.07\times10^{10}$), all FLOPs counted. The
    <span class="swatch fast_weight"></span>`delta` point (counts + the delta stream) sits **below**
    both the <span class="swatch reference"></span>`counts_only` ladder and the
    <span class="swatch warm"></span>`counts_more_warm` control. Because the three runs are matched on
    total FLOPs they cluster in x (a hair apart on a log-FLOP axis, as in
    [B.1](/experiments/B.1-surprise-gated-pc-refinement)) — the **vertical** separation carries the
    result; the exact numbers are in the table. The binding pair is **(b) vs (c)**. The full-corpus
    headline (`delta_o6_warmfull`) is **pending** (below) and is not plotted — no invented coordinate.
  </figcaption>
</figure>

**Verdict: PASS** — `delta` beats **both** (a) and (c). The binding comparison is **(b) vs (c)** at
matched total FLOPs: spending the delta stream's FLOPs on the learning rule beats spending them on more
warm count bytes by **−0.0146 bpb**. Small, but on the right side of the line A.1 / B.1 / B.2-gated all
fell on.

### Two mechanistic diagnostics (why it is real, not noise)

<Callout variant="insight" title="the mixer trusts it (+0.8595)">
The final learned mixer weight on the delta stream is **+0.8595** — strongly load-bearing, the
**opposite** of A.1's tell (there the gate weight was ~identical trained-vs-untrained, i.e. dead). The
ensemble *chose* to lean on the delta stream.
</Callout>

And it generalizes exactly where the counts have nothing. On **20,051** eval contexts whose exact
order-6 n-gram was never seen during warm — so the order-6 count **abstains** — the delta stream alone
still scores **3.73 bpb**:

<figure class="figure">
  <div class="genbars">
    <div class="genbar-row">
      <span class="genbar-label">delta stream alone</span>
      <span class="genbar-track"><span class="genbar-fill delta" style="width:46.6%">3.73 bpb</span></span>
    </div>
    <div class="genbar-row">
      <span class="genbar-label">order-6 count (abstains)</span>
      <span class="genbar-track"><span class="genbar-fill abstain" style="width:100%">8.0 bpb</span></span>
    </div>
  </div>
  <figcaption class="figcaption">
    On 20,051 contexts whose exact order-6 n-gram was unseen during warm, the order-6 count
    **abstains** — scoring the uniform **8.0 bpb** "no model" floor — while the **delta stream alone**
    scores **3.73 bpb** (lower is better, so shorter is better). A distributed key carries signal from
    seen sub-n-grams exactly where an exact-match table has nothing: the (iv) generalization edge made
    visible.
  </figcaption>
</figure>

## Full-corpus carve — pending

The kill-test is **CI scale** (4 MB slice, $\approx 1\times10^{10}$ FLOPs). Whether the win survives at
the full-corpus **$1.48\times10^{12}$** bar — where the count tables saturate more and abstain less,
possibly shrinking the delta edge — is the `delta_o6_warmfull` carve, which was still running detached
when this page was built:

| run | bpb | total FLOPs | measured against |
| --- | ---: | ---: | --- |
| `delta_o6_warmfull` (full 95 MB warmup) | *pending* | $\approx 1.48\times10^{12}$ | the **2.0157** bpb [B.3](/experiments/B.3-hashed-mix-full-corpus) bar |

<Callout variant="caveat" title="the full-carve verdict is not in">
This row carries **no number**. `runs/full/leaderboard.md` had no `delta_o6_warmfull` entry when this
page was built, so it stays *pending* rather than fabricate a result — honest either way. The CI-scale
win is **real but modest** (−0.0146 bpb at matched FLOPs); it is the first Space-B result to clear the
cheap-baseline bar **at all**. The downside is bounded: error-correction prevents A.1's
marginal-collapse, and the online mixer drives a useless stream's weight → 0 with no bpb blow-up.
</Callout>

## What we learned

1. **Attack the baseline's *structure*, not its budget.** Every Space-B candidate that refined an
   already-cheap learner stayed Pareto-hollow. Targeting the count mixer's two hard limits — zero
   generalization and only $K$ global weights — is what finally produced a non-hollow result.
2. **The write rule is the whole story.** Error-correcting delta (LMS) beats the Hebbian write that
   killed A.1: it stops moving a feature's weight once it predicts well, so a superposed key does not
   collapse to the byte marginal (an ablation confirms it).
3. **A distributed key generalizes where exact-match counts abstain** — the +0.8595 mixer weight and the
   3.73-vs-8.0 unseen-context split are the same mechanism seen two ways.
4. **The verdict is honestly partial.** A modest CI-scale win is established; the full-corpus carve is
   pending, and the win could shrink where the count tables saturate. Logged as such.

<Callout variant="note" title="scale & reproduce">
CI-fast kill-test (downloads `enwik8` once; minutes): `uv run python -m smolml.experiments.delta_mix_enwik8`.
The full ADR-carve headline (multi-hour, detached) runs the cast incl. `delta_o6_warmfull` via
`uv run python -m smolml.experiments.full_corpus`. Model: `smolml/models/delta_mix.py`; tests:
`tests/test_delta_mix.py`.
</Callout>

## See also

- [Fast-weight associative memory](/concepts/fast-weight-memory) — the mechanism and the two write-rule lineages (Hebbian bolt-on vs delta-rule stream).
- [A.1 — fast-weight memory](/experiments/A.1-fast-weight-memory) — the Hebbian bolt-on this candidate is the error-correcting answer to.
- [B.3 — hashed tables at full corpus](/experiments/B.3-hashed-mix-full-corpus) — the warmed hashed ladder the delta stream rides on, and the 2.0157 bar.
- [B.2 — warmed mixing (first real win)](/experiments/B.2-warmed-mixing) — the warm-start the count tables use.
- [Online context mixing](/concepts/context-mixing) — the logistic mixer the delta stream joins as one more logit.
- [Source-(iv) advantage](/concepts/source-iv-advantage) — the only kind of win a non-backprop idea may claim.
- [Prequential evaluation](/concepts/prequential-evaluation) — the protocol that charges its adaptation FLOPs.

</Layout>
